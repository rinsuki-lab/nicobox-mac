# Uncomment the next line to define a global platform for your project
platform :osx, '10.14'
use_frameworks!

target 'nicobox-mac' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks

  # Pods for nicobox-mac
  pod 'Fuzi', '~> 2.0.0'
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'
end
